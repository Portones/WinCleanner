<UserControl x:Class="WinCleaner.Views.CleanupHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WinCleaner.ViewModels"
             xmlns:controls="clr-namespace:WinCleaner.Views.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:CleanupHistoryViewModel}"
             d:DesignHeight="650" d:DesignWidth="900">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- CABECERA -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="📊 Historial Gráfico de Limpieza" FontSize="26" FontWeight="Bold" Foreground="#F8FAFC"/>
            <TextBlock Text="Monitorea el espacio liberado en disco por fecha y consulta el histórico detallado de tareas de limpieza." FontSize="12" Foreground="#888888" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- TARJETAS MÉTRICAS -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="TOTAL HISTÓRICO LIBERADO" FontSize="10" FontWeight="Bold" Foreground="#94A3B8"/>
                    <TextBlock Text="{Binding TotalCleanedSizeText}" FontSize="22" FontWeight="Bold" Foreground="#6366F1" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="OPERACIONES REALIZADAS" FontSize="10" FontWeight="Bold" Foreground="#94A3B8"/>
                    <TextBlock Text="{Binding TotalOperationsCount}" FontSize="22" FontWeight="Bold" Foreground="#10B981" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="PROMEDIO POR LIMPIEZA" FontSize="10" FontWeight="Bold" Foreground="#94A3B8"/>
                    <TextBlock Text="{Binding AverageCleanedSizeText}" FontSize="22" FontWeight="Bold" Foreground="#8B5CF6" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- PANEL DE LA GRÁFICA DE BARRAS -->
        <Border Grid.Row="2" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="10" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="200"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="Espacio Liberado en los Últimos 7 Días" FontSize="14" FontWeight="Bold" Foreground="#F8FAFC"/>
                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                        <Button Content="🔄 Recargar" Command="{Binding RefreshCommand}" Style="{StaticResource SidebarButton}" Margin="0,0,6,0"/>
                        <Button Content="🗑️ Borrar Historial" Command="{Binding ClearHistoryCommand}" Style="{StaticResource SidebarButton}" Foreground="#EF4444"/>
                    </StackPanel>
                </Grid>

                <controls:HistoryChartControl Grid.Row="1"/>
            </Grid>
        </Border>

        <!-- TABLA DE REGISTROS HISTÓRICOS -->
        <Border Grid.Row="3" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="10" Padding="10">
            <ListView ItemsSource="{Binding HistoryItems}" Background="Transparent" BorderThickness="0" Foreground="#F8FAFC">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Fecha / Hora" Width="180" DisplayMemberBinding="{Binding DateText}"/>
                        <GridViewColumn Header="Espacio Liberado" Width="200" DisplayMemberBinding="{Binding SizeText}"/>
                        <GridViewColumn Header="Elementos Limpiados" Width="200" DisplayMemberBinding="{Binding ItemsCount}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>
