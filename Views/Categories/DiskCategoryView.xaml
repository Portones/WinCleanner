<UserControl x:Class="WinCleaner.Views.Categories.DiskCategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:WinCleaner.Views"
             xmlns:vm="clr-namespace:WinCleaner.ViewModels"
             xmlns:catVm="clr-namespace:WinCleaner.ViewModels.Categories"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance catVm:DiskCategoryViewModel}"
             d:DesignHeight="650" d:DesignWidth="900">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:DiskAnalyzerViewModel}">
            <local:DiskAnalyzerView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SsdOptimizerViewModel}">
            <local:SsdOptimizerView/>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="5">
        <TabControl Style="{StaticResource ModernCategoryTabControl}">
            <TabItem Header="📊 Analizador de Disco" Content="{Binding DiskAnalyzerViewModel}"/>
            <TabItem Header="💽 Optimizador TRIM" Content="{Binding SsdOptimizerViewModel}"/>
        </TabControl>
    </Grid>
</UserControl>
