<UserControl x:Class="WinCleaner.Views.Categories.AppCategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:WinCleaner.Views"
             xmlns:vm="clr-namespace:WinCleaner.ViewModels"
             xmlns:catVm="clr-namespace:WinCleaner.ViewModels.Categories"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance catVm:AppCategoryViewModel}"
             d:DesignHeight="650" d:DesignWidth="900">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:UninstallerViewModel}">
            <local:UninstallerView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:UpdaterViewModel}">
            <local:UpdaterView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RuntimeInstallerViewModel}">
            <local:RuntimeInstallerView/>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="5">
        <TabControl Style="{StaticResource ModernCategoryTabControl}">
            <TabItem Header="🗑️ Desinstalador" Content="{Binding UninstallerViewModel}"/>
            <TabItem Header="🔄 Actualizar Apps" Content="{Binding UpdaterViewModel}"/>
            <TabItem Header="📦 Instalador Runtimes" Content="{Binding RuntimeInstallerViewModel}"/>
        </TabControl>
    </Grid>
</UserControl>
