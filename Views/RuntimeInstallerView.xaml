<UserControl x:Class="WinCleaner.Views.RuntimeInstallerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WinCleaner.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:RuntimeInstallerViewModel}"
             d:DesignHeight="650" d:DesignWidth="900">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="160"/>
        </Grid.RowDefinitions>

        <!-- CABECERA -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="📦 Instalador de Runtimes y Dependencias" FontSize="26" FontWeight="Bold" Foreground="#F8FAFC"/>
            <TextBlock Text="Instala de forma desatendida las librerías C++, entornos .NET y paquetes multimedia esenciales para juegos y software." FontSize="12" Foreground="#888888" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- BARRA DE ACCIONES Y PROGRESO -->
        <Border Grid.Row="1" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="10" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding StatusMessage}" FontSize="13" FontWeight="SemiBold" Foreground="#F8FAFC"/>
                    <ProgressBar Style="{StaticResource ModernProgressBar}" Value="{Binding ProgressValue}" Maximum="100" Margin="0,8,20,0" Height="8"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="🔄 Actualizar" Command="{Binding RefreshCommand}" Style="{StaticResource SidebarButton}" Margin="0,0,6,0"/>
                    <Button Content="✓ Seleccionar Todos" Command="{Binding SelectAllCommand}" Style="{StaticResource SidebarButton}" Margin="0,0,6,0"/>
                    <Button Content="✗ Desmarcar Todos" Command="{Binding DeselectAllCommand}" Style="{StaticResource SidebarButton}" Margin="0,0,10,0"/>

                    <Button Content="⬇️ Instalar Seleccionados"
                            Command="{Binding InstallSelectedCommand}"
                            Style="{StaticResource ActionButton}"
                            Margin="0,0,8,0"/>

                    <Button Content="🛑 Cancelar"
                            Command="{Binding CancelCommand}"
                            Style="{StaticResource ActionButton}"
                            Background="#EF4444"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- LISTA DE RUNTIMES -->
        <Border Grid.Row="2" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="10" Padding="10" Margin="0,0,0,15">
            <ListView ItemsSource="{Binding Runtimes}" Background="Transparent" BorderThickness="0" Foreground="#F8FAFC">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#0F172A" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,12,0"/>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Bold" Foreground="#F8FAFC"/>
                                        <Border Background="#312E81" BorderBrush="#4F46E5" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="10,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Category}" FontSize="10" FontWeight="SemiBold" Foreground="#A5B4FC"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Description}" FontSize="11.5" Foreground="#94A3B8" Margin="0,3,0,0" TextWrapping="Wrap"/>
                                </StackPanel>

                                <Border Grid.Column="2" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="6" Padding="10,5" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Status}" FontSize="12" FontWeight="Bold" Foreground="#6366F1"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- CONSOLA TERMINAL DE INSTALACIÓN -->
        <Border Grid.Row="3" Background="#0B0F19" BorderBrush="#1E293B" BorderThickness="1" CornerRadius="10" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,6">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="10" Height="10" Fill="#EF4444" Margin="0,0,6,0"/>
                        <Ellipse Width="10" Height="10" Fill="#F59E0B" Margin="0,0,6,0"/>
                        <Ellipse Width="10" Height="10" Fill="#10B981" Margin="0,0,10,0"/>
                        <TextBlock Text="Histórico de Instalación Winget" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                    </StackPanel>
                    <Button Content="Limpiar" Command="{Binding ClearLogCommand}" HorizontalAlignment="Right" Style="{StaticResource SidebarButton}" Padding="8,2" FontSize="11"/>
                </Grid>

                <TextBox x:Name="ConsoleLog"
                         Grid.Row="1"
                         Text="{Binding ConsoleLog, Mode=OneWay}"
                         TextChanged="ConsoleLog_TextChanged"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Background="Transparent"
                         Foreground="#10B981"
                         FontFamily="Consolas, Cascadia Code, Courier New"
                         FontSize="11.5"
                         BorderThickness="0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
