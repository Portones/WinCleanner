<UserControl x:Class="WinCleaner.Views.RamOptimizerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WinCleaner.Views"
             xmlns:vm="clr-namespace:WinCleaner.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             x:Name="RamOptimizerRoot">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Estilo de cabecera de columna -->
        <Style x:Key="ColumnHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <!-- Cabecera -->
            <RowDefinition Height="Auto"/>
            <!-- Tarjetas de Estado y Acción -->
            <RowDefinition Height="Auto"/>
            <!-- Caja de búsqueda y controles de filtro -->
            <RowDefinition Height="Auto"/>
            <!-- Tabla de Procesos -->
            <RowDefinition Height="*"/>
            <!-- Pie de Estado -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- CABECERA -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Optimizar RAM y Procesos Activos" FontSize="26" FontWeight="Bold" Foreground="#F8FAFC"/>
                <TextBlock Text="Supervise el consumo de memoria en tiempo real, finalice procesos colgados y libere memoria RAM inactiva al instante." FontSize="12" Foreground="#888888" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <!-- TARJETAS DE ESTADO Y ACCIÓN -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Tarjeta 1: Estado de RAM -->
            <Border Grid.Column="0" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="12" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Path Data="M2 20h20L12 4 2 20zm10-3h-2v-2h2v2zm0-4h-2v-4h2v4z" Fill="#10B981" Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Estado de Memoria Física (RAM)" FontSize="15" FontWeight="Bold" Foreground="#F8FAFC" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" VerticalAlignment="Center">
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="Porcentaje de uso actual" FontSize="11" Foreground="#94A3B8"/>
                            <TextBlock Text="{Binding RamUsagePercentage, StringFormat={}{0}%}" FontSize="11" FontWeight="Bold" Foreground="#10B981" HorizontalAlignment="Right"/>
                        </Grid>
                        <!-- ProgressBar usando estilo premium del sistema -->
                        <ProgressBar Value="{Binding RamUsagePercentage}" Style="{StaticResource RamPhysicalProgressBar}" Height="10" Margin="0,0,0,12"/>
                        <TextBlock Text="{Binding RamUsageText}" FontSize="12" Foreground="#64748B" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Tarjeta 2: Smart RAM Booster -->
            <Border Grid.Column="2" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="12" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Path Data="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z" Fill="#6366F1" Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Smart RAM Booster" FontSize="15" FontWeight="Bold" Foreground="#F8FAFC" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Compacta la memoria de trabajo de procesos de fondo no utilizados de manera segura, liberando espacio para tus programas activos." FontSize="11.5" Foreground="#94A3B8" TextWrapping="Wrap" Margin="0,0,0,10"/>

                        <StackPanel Grid.Row="1">
                            <!-- Progreso de optimización -->
                            <Grid Margin="0,0,0,8" Visibility="{Binding IsOptimizing, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ProgressBar Value="{Binding OptimizationProgress}" Style="{StaticResource RamCommittedProgressBar}" Height="6"/>
                            </Grid>
                            
                            <Button Content="Optimizar RAM (Smart Boost)" 
                                    Command="{Binding OptimizeRamCommand}"
                                    IsEnabled="{Binding CanOptimize}"
                                    Style="{StaticResource ActionButton}"
                                    HorizontalAlignment="Stretch" Height="36" Padding="0"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- FILTROS Y CONTROLES -->
        <Border Grid.Row="2" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="12" Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Caja de búsqueda -->
                <Grid Grid.Column="0" MaxWidth="350" HorizontalAlignment="Left" Width="300">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             Background="#0F172A" BorderBrush="#334155" BorderThickness="1" 
                             Foreground="#F8FAFC" FontSize="12.5" Padding="32,6,10,6" Height="34">
                        <TextBox.Resources>
                            <Style TargetType="{x:Type Border}">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>
                    <!-- Icono lupa -->
                    <Path Data="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z" 
                          Fill="#64748B" Width="14" Height="14" Stretch="Uniform" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    <!-- Placeholder text -->
                    <TextBlock Text="Buscar procesos..." IsHitTestVisible="False" Margin="34,0,0,0" VerticalAlignment="Center" FontSize="12.5" Foreground="#64748B">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SearchText}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <!-- Actualización Automática -->
                <CheckBox Grid.Column="1" IsChecked="{Binding IsAutoRefreshEnabled, Mode=TwoWay}" VerticalAlignment="Center" Margin="15,0,0,0">
                    <TextBlock Text="Actualizar automáticamente (2s)" FontSize="12" Foreground="#94A3B8"/>
                </CheckBox>

                <!-- Recargar manual -->
                <Button Grid.Column="2" Content="Refrescar" Command="{Binding RefreshCommand}" Style="{StaticResource SidebarButton}" Height="34" Padding="15,0" Margin="15,0,0,0"/>
            </Grid>
        </Border>

        <!-- TABLA DE PROCESOS ACTIVES -->
        <Border Grid.Row="3" Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="12" Padding="0" ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Cabecera de columnas personalizada -->
                <Border Grid.Row="0" Background="#0B0F19" Padding="12,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1.2*"/>
                            <ColumnDefinition Width="1.2*"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="PID" Style="{StaticResource ColumnHeaderStyle}"/>
                        <TextBlock Grid.Column="1" Text="NOMBRE" Style="{StaticResource ColumnHeaderStyle}"/>
                        <TextBlock Grid.Column="2" Text="USO MEMORIA (RAM)" Style="{StaticResource ColumnHeaderStyle}"/>
                        <TextBlock Grid.Column="3" Text="USO CPU" Style="{StaticResource ColumnHeaderStyle}"/>
                        <TextBlock Grid.Column="4" Text="ACCIONES" Style="{StaticResource ColumnHeaderStyle}" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Filas de datos -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Processes}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12,9" BorderBrush="#1A2540" BorderThickness="0,0,0,1">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#1A2D4A"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="1.2*"/>
                                            <ColumnDefinition Width="1.2*"/>
                                            <ColumnDefinition Width="150"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- PID -->
                                        <TextBlock Grid.Column="0" Text="{Binding Id}" FontSize="12.5" Foreground="#64748B" VerticalAlignment="Center"/>

                                        <!-- Nombre -->
                                        <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" Foreground="#F1F5F9" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>

                                        <!-- Uso Memoria -->
                                        <TextBlock Grid.Column="2" Text="{Binding MemoryText}" FontSize="12.5" Foreground="#10B981" FontWeight="SemiBold" VerticalAlignment="Center"/>

                                        <!-- Uso CPU -->
                                        <TextBlock Grid.Column="3" Text="{Binding CpuText}" FontSize="12.5" Foreground="#818CF8" FontWeight="SemiBold" VerticalAlignment="Center"/>

                                        <!-- Botón Finalizar -->
                                        <Button Grid.Column="4" 
                                                Command="{Binding DataContext.KillProcessCommand, ElementName=RamOptimizerRoot}"
                                                CommandParameter="{Binding}"
                                                HorizontalAlignment="Center" VerticalAlignment="Center" Height="28">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="bg" CornerRadius="5" Padding="12,0" Background="#3F0F0F" BorderBrush="#7F1D1D" BorderThickness="1">
                                                        <TextBlock Text="Finalizar" FontSize="11" FontWeight="Bold" Foreground="#FCA5A5" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="bg" Property="Background" Value="#7F1D1D"/>
                                                            <Setter TargetName="bg" Property="BorderBrush" Value="#EF4444"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter TargetName="bg" Property="Background" Value="#991B1B"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- PIE DE ESTADO -->
        <Border Grid.Row="4" Background="#0B0F19" BorderBrush="#334155" BorderThickness="0,1,0,0" Padding="12,10" Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#94A3B8" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
